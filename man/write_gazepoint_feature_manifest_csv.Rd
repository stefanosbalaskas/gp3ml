% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-provenance-manifest.R
\name{write_gazepoint_feature_manifest_csv}
\alias{write_gazepoint_feature_manifest_csv}
\title{Write a Gazepoint feature manifest or validation table to CSV}
\usage{
write_gazepoint_feature_manifest_csv(
  x,
  file,
  table = c("manifest", "issues", "checks"),
  overwrite = FALSE,
  na = ""
)
}
\arguments{
\item{x}{A \code{gazepoint_feature_manifest}, compatible data frame, or
object returned by \code{\link[=validate_gazepoint_feature_manifest]{validate_gazepoint_feature_manifest()}}.}

\item{file}{A single output path ending in \code{.csv}.}

\item{table}{Table to export. One of \code{"manifest"}, \code{"issues"}, or
\code{"checks"}. Plain manifest inputs support only \code{"manifest"}.}

\item{overwrite}{Logical. When \code{FALSE}, the default, an existing file
causes an error.}

\item{na}{Character value used for missing values.}
}
\value{
The normalized output path, invisibly.
}
\description{
Writes a feature manifest or one table from a validated manifest to a
UTF-8 CSV file. Existing files are not replaced unless explicitly
permitted.
}
\examples{
manifest <- create_gazepoint_feature_manifest(
  features = "fixation_duration",
  scientific_source = "Gazepoint fixation export",
  source_table = "fixations",
  transformation = "Trial-level mean",
  availability_stage = "during_exposure",
  prediction_time_available = TRUE,
  preprocessing_scope = "none",
  fold_local_required = FALSE
)

output <- tempfile(fileext = ".csv")

write_gazepoint_feature_manifest_csv(
  manifest,
  output
)

unlink(output)

}
