% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-tuning.R
\name{tune_gazepoint_model}
\alias{tune_gazepoint_model}
\alias{print.gp3ml_model_tuning}
\title{Evaluate every governed candidate on the same grouped folds}
\usage{
tune_gazepoint_model(
  folds,
  task,
  tuning_grid,
  predictors = NULL,
  metrics = NULL,
  seed = 1L,
  continue_on_error = TRUE,
  keep_evaluations = TRUE
)

\method{print}{gp3ml_model_tuning}(x, ...)
}
\arguments{
\item{folds}{A \code{gazepoint_group_folds} object.}

\item{task}{A governed task.}

\item{tuning_grid}{A \code{gp3ml_tuning_grid}.}

\item{predictors}{Optional declared predictors.}

\item{metrics}{Optional metric names retained in the comparison table.}

\item{seed}{Base deterministic seed.}

\item{continue_on_error}{Whether failed candidates remain in the result while
later candidates continue.}

\item{keep_evaluations}{Whether complete candidate evaluations are retained.}

\item{x}{An object returned by the corresponding gp3ml constructor, evaluator, summarizer, or validator.}

\item{...}{Additional arguments passed to the print method.}
}
\value{
A \code{gp3ml_model_tuning} object retaining all candidates and failures.
}
\description{
Evaluate every governed candidate on the same grouped folds
}
\examples{
data <- simulate_gazepoint_governed_data(12L, 4L, 1L, 202L)
predictors <- c("tracking_ratio", "blink_rate", "gaze_dispersion")
manifest <- create_gazepoint_synthetic_manifest("quality_status", predictors)
folds <- create_gazepoint_group_folds(
  data, "quality_status", predictors, manifest,
  "new_participants", "participant_id", "trial_id", "stimulus_id",
  v = 3L, repeats = 1L, seed = 202L
)
task <- create_gazepoint_synthetic_task(data, "recording_quality", "new_participants")
grid <- create_gazepoint_tuning_grid(
  "glm",
  preprocessor_grid = list(center = c(TRUE, FALSE), scale = TRUE),
  thresholds = 0.5
)
tuned <- tune_gazepoint_model(folds, task, grid, predictors, seed = 202L)
tuned
}
