% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample-evaluation.R
\name{evaluate_gazepoint_group_folds}
\alias{evaluate_gazepoint_group_folds}
\alias{print.gp3ml_resample_evaluation}
\title{Evaluate a governed model specification across materialized grouped folds}
\usage{
evaluate_gazepoint_group_folds(
  folds,
  task,
  predictors = NULL,
  engine = NULL,
  preprocessor_args = list(),
  engine_args = list(),
  threshold = 0.5,
  seed = 1L,
  assess_calibration = FALSE,
  calibration_bins = 10L,
  calibration_bootstrap = 0L,
  keep_models = FALSE,
  continue_on_error = TRUE
)

\method{print}{gp3ml_resample_evaluation}(x, ...)
}
\arguments{
\item{folds}{A mature \code{gazepoint_group_folds} object containing materialized
folds under \code{folds$folds}.}

\item{task}{A governed \code{gp3ml_task} compatible with the fold metadata.}

\item{predictors}{Optional predictor names. Defaults to the fold metadata.}

\item{engine}{Model engine name or governed custom engine.}

\item{preprocessor_args}{Arguments passed to \code{\link[=fit_gazepoint_preprocessor]{fit_gazepoint_preprocessor()}}.}

\item{engine_args}{Arguments passed to \code{\link[=fit_gazepoint_model]{fit_gazepoint_model()}}.}

\item{threshold}{Classification threshold.}

\item{seed}{Base deterministic seed.}

\item{assess_calibration}{Whether to calculate assessment-fold calibration
summaries for classification tasks.}

\item{calibration_bins}{Number of reliability bins.}

\item{calibration_bootstrap}{Calibration bootstrap replicates. Use zero in
fast smoke tests.}

\item{keep_models}{Whether fitted fold models are retained.}

\item{continue_on_error}{Whether later folds continue after a failed fold.}

\item{x}{An object returned by the corresponding gp3ml constructor, evaluator, summarizer, or validator.}

\item{...}{Additional arguments passed to the print method.}
}
\value{
A \code{gp3ml_resample_evaluation} object.
}
\description{
Fits preprocessing and the requested model only on each fold's analysis
partition, predicts only on the corresponding assessment partition, retains
excluded rows, and records fold-level metrics, leakage audits, warnings, and
failures. Row-level predictions are never relabelled as participant- or
stimulus-level estimates.
}
\examples{
data <- simulate_gazepoint_governed_data(12L, 4L, 1L, 101L)
predictors <- c("tracking_ratio", "blink_rate", "gaze_dispersion")
manifest <- create_gazepoint_synthetic_manifest("quality_status", predictors)
folds <- create_gazepoint_group_folds(
  data = data,
  outcome = "quality_status",
  predictors = predictors,
  feature_manifest = manifest,
  generalization_target = "new_participants",
  participant_id = "participant_id",
  trial_id = "trial_id",
  stimulus_id = "stimulus_id",
  v = 3L,
  repeats = 1L,
  seed = 101L
)
task <- create_gazepoint_synthetic_task(
  data,
  "recording_quality",
  "new_participants"
)
evaluation <- evaluate_gazepoint_group_folds(
  folds,
  task,
  predictors = predictors,
  engine = "glm",
  seed = 101L
)
evaluation
}
