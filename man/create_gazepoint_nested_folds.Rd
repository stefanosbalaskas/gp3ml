% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-resampling.R
\name{create_gazepoint_nested_folds}
\alias{create_gazepoint_nested_folds}
\alias{print.gp3ml_nested_folds}
\title{Create nested grouped resampling from mature outer folds}
\usage{
create_gazepoint_nested_folds(
  outer_folds,
  inner_v = 3L,
  inner_repeats = 1L,
  seed = 1L,
  continue_on_error = FALSE
)

\method{print}{gp3ml_nested_folds}(x, ...)
}
\arguments{
\item{outer_folds}{A validated \code{gazepoint_group_folds} object.}

\item{inner_v}{Number of inner folds.}

\item{inner_repeats}{Number of inner repeats.}

\item{seed}{Base deterministic seed.}

\item{continue_on_error}{Whether infeasible outer folds are retained as
failures instead of stopping immediately.}

\item{x}{An object returned by the corresponding gp3ml constructor, evaluator, summarizer, or validator.}

\item{...}{Additional arguments passed to the print method.}
}
\value{
A \code{gp3ml_nested_folds} object.
}
\description{
Inner folds are constructed only from each outer analysis partition and
preserve the declared participant/stimulus generalization target. The outer
assessment partition is never used for inner preprocessing or tuning.
}
\examples{
data <- simulate_gazepoint_governed_data(16L, 4L, 1L, 303L)
predictors <- c("tracking_ratio", "blink_rate", "gaze_dispersion")
manifest <- create_gazepoint_synthetic_manifest("quality_status", predictors)
outer <- create_gazepoint_group_folds(
  data, "quality_status", predictors, manifest,
  "new_participants", "participant_id", "trial_id", "stimulus_id",
  v = 4L, repeats = 1L, seed = 303L
)
nested <- create_gazepoint_nested_folds(
  outer,
  inner_v = 3L,
  inner_repeats = 1L,
  seed = 303L
)
nested
}
