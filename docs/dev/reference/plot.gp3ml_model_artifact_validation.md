# Plot model-artifact validation

Plot model-artifact validation

## Usage

``` r
# S3 method for class 'gp3ml_model_artifact_validation'
plot(x, ...)
```

## Arguments

- x:

  A `gp3ml_model_artifact_validation`.

- ...:

  Additional arguments passed to
  [`graphics::barplot()`](https://rdrr.io/r/graphics/barplot.html).
